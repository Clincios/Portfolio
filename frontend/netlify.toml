[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Handle client-side routing (for React Router if you add it later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
